import type { MapARSceneProps } from './mapARSceneProps';

/** Production stub — real MapARScene excluded from release bundle (see metro.config.js). */
export default function MapARSceneEmpty(_props: MapARSceneProps) {
  return null;
}

MapARSceneEmpty.preload = () => {};
