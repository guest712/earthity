# Copy to .env in project root (not committed).
# EAS Build: set the same names in expo.dev → Environment variables → profile preview.

EXPO_PUBLIC_SUPABASE_URL=https://xxxx.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=eyJ...

# Baked into native Android/iOS at eas build via app.config.js
GOOGLE_MAPS_API_KEY=
